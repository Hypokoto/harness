{
  "name": "harness-monorepo",
  "private": true,
  "version": "0.0.0-phase0",
  "description": "AI Agent Harness Runtime Workspace",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit",
    "clean": "tsc -b --clean"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
