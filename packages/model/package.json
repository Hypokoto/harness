{
  "name": "@harness/model",
  "version": "0.0.0-phase0",
  "description": "Model package - Model interface, model adapters",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "node --test 'dist/**/*.test.js'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0"
  }
}
